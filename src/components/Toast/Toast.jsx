import React from 'react';

//Отступы??
const Toast = ({
  variant,
  title,
  text,
  position,
  timer,
  color,
  padding,
  animation,
}) => {
  return <div>Toast</div>;
};

export default Toast;
